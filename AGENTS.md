# AGENTS.md

Codex はこのリポジトリでは**監視役**を務める。書く側は Claude Code、見る側が Codex という
分担になっている。

## 守るルール

このリポジトリの決まりごとは `CLAUDE.md` に、作業手順は `.claude/skills/*/SKILL.md` に書いてある。
Codex もそれを正とする。ここに同じことを二重に書かない（食い違うので）。

## Codex の役割

- **やること** — 差分の校閲と監査。CLAUDE.md とスキルのルールが守れているかを見る。
- **やらないこと** — ファイルの書き換え。台本や記事の代筆。手順の勝手な変更。

見るべき点は優先順に次のとおり。

1. 数字（価格・レート・日付）に出典があるか。出典ノートへの `[[リンク]]` が張られているか。
2. 記憶や推測で書いたと疑われる数字が混ざっていないか。
3. 断定できないことを断定していないか。
4. 常体／敬体、一人称、文量がスキルの指定どおりか。
5. 成果物が所定のフォルダにファイルとして保存されているか。
6. Obsidian のノートとして壊れていないか（frontmatter、リンク切れ、ファイル名に使えない文字）。

指摘は「該当ファイル名 + 何が問題か」の形で書く。直し方の提案は一言で足りる。

## 呼ばれ方

- Claude Code が一区切りつくたび、Stop フック（`.claude/hooks/codex-review.sh`）が
  `codex exec --sandbox read-only` で自動的に呼ぶ。結果は `brain/reviews/` に溜まる。
- 手で見たいときは `codex review --uncommitted` を使う。

## 過去の指摘

`brain/reviews/` に日付順で残っている。同じ指摘が三回出たら、それは個別の間違いではなく
ルールの書き漏れなので、CLAUDE.md か該当スキルに書き足す。
